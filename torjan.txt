nova pasta
cmd
code .

npm init -y : criar um projeto com as informações padrão

npm install express ejs sequelize mysql2 nodemon : instalação das bibliotecas

package.json : inserir em scripts o start : "start": "npx nodemon index.js"

inserir antes do ultimo colchetes: "type" : "module" 

criar o arquivo principal: index.js e começar a programar;

import express from "express";

const app = express()

const port = 8080;
app.listen(port, (error)=> {
    if(error){
        console.log(Ocorreu um erro: ${error})
    } else {
        console.log(Servidor iniciado em http://localhost:${port})
    }
});

//agora vai no terminal e npm start
//criar as pastas models, views e controllers (pastas em nomes minúsculos)